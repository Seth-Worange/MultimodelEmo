# 问题二、三的瓶颈诊断与下一轮实验计划

日期：2026-09-24。依据：重新阅读赛题原文及用户补充答疑，检查当前代码，使用现有检查点重算附件2验证集，并完成训练集诊断。完整数据记录为 `outputs/diagnostics/bottleneck_audit_20260924/audit.json`；检查点路径及 SHA256 已记录。本次没有使用附件2 test 指标选型，没有训练附件3/4伪标签，没有引入外部情感数据。

结论：当前优先级应是评估一致性、输入尺度与有效长度、分类/回归目标、CICA 单模态预训练质量、解释覆盖与忠实度。现有证据不支持继续加深网络或强行提高声画门控权重。以下区分已确认的问题、机制假设、已经修正的功能和仍需重训的实验。

## 1. 重新对齐任务

问题二的主要场景是局部连续时段缺失；整模态丢弃可以作训练正则和极端压力测试，但不应占据选模目标的同等地位而不加说明。当前 `selection_score` 对 clean/local/whole/interval 四个视图等权平均；interval 验证包含文本缺失，而部分训练配置只允许声画缺失。后续应固定定义并分别报告，避免把不同场景平均成一个无法解释的分数。

问题三要求完整模态情感识别及可解释证据。它可以共享问题二的特征编码器，但应以完整验证集预测质量选择候选，再以独立的解释忠实度实验评估证据。不能直接把问题二抗缺失的综合最优检查点等同于问题三最优。

三分类规则是 `y<0 / y==0 / y>0`，中性不能并入正向。验证集用于模型选择和报告题目要求的指标；附件3/4无标签，只输出结果和解释。两种粒度可同时使用，但对齐器只能利用训练数据拟合，对训练、验证、专项样本采用统一处理。

论文中的 MOSEI 二分类 Acc2 与本题三分类 Accuracy 不能直接比较。CICA 原文的 MOSEI 训练集为16,326条，本题实际训练集只有3,395条；预处理、划分和目标也不同。这里不能以“论文86%，所以当前模型还差21个百分点”的方式判断瓶颈。

## 2. 当前可追溯的验证结果

全部为附件2 valid、728条、完整输入、原始输出规则。BiGRU基线和FUSE是两个既有种子的集成，其他行为单模型；此表是现有候选比较，不是严格控制变量的架构消融。

| 检查点 | Accuracy | Macro-F1 | MAE | Pearson |
|---|---:|---:|---:|---:|
| 基线 BiGRU：availability_s2026+s2027 | 0.6319 | 0.6192 | 0.5804 | 0.6462 |
| 基线 Transformer：baseline_transformer_s2026 | 0.6126 | 0.5929 | 0.5851 | 0.6687 |
| FUSE：fuse_s2026+s2027 | **0.6497** | **0.6328** | **0.5729** | **0.6737** |
| CICA 新 BiGRU：cica_softca_bigru_s20260924 | 0.6126 | 0.5952 | 0.6077 | 0.6223 |
| CICA 新 Transformer：cica_softca_transformer_s20260924 | 0.6168 | 0.5903 | 0.6241 | 0.6091 |
| CICA 旧版：cica_s2027 | 0.6016 | 0.5915 | 0.5840 | 0.6497 |
| 文本单模态：text_only | 0.6346 | 0.6069 | 0.6040 | 0.6494 |

记录纠正：`outputs/runs/availability/validation_metrics.json` 的指标与基线 Transformer 单模型一致，并不对应目前 `q2.yaml` 引用的 BiGRU 双种子检查点。历史文件保留，后续使用本次按权重重算的记录；新评估报告保存实际权重路径、哈希和结构配置。

## 3. 已确认的主要问题

### 3.1 输入尺度悬殊，尾部补零参与了序列编码

只统计训练集实际有效位置，音频74维各维标准差的最大/最小比约 **2333**，视觉35维约 **23.3**。代码使用 `Linear → LayerNorm → GELU`，没有在投影前做训练集逐特征标准化。投影后的 LayerNorm 并不能消除投影前某些坐标对线性组合的支配。

三种结构的音视频 BiGRU、融合 BiGRU 都存在运行完整50步后再掩码池化的路径。零输入经过带偏置投影和反向GRU后，尾部 padding 会影响真实位置。16条样本诊断中，FUSE音频有效隐藏状态在裁去尾部padding后平均绝对变化为0.0151；基线BiGRU为0.0324。**这证明对padding敏感，还没有证明修复能提高验证分数。** 模态编码器换成Transformer，也没有自动解决保留的融合GRU问题。

下一步：从train有效位置拟合音频/视觉每维均值和标准差并随检查点保存；先识别缺失，变换后把缺失位置重新置零。音频差分在标准化后的有效相邻位置计算。GRU按原始序列长度pack，不能用缺失后的有效位置数量代替原始长度，否则会把内部缺口压缩掉。标准化和padding修正分别作消融。

#### P1 实际训练筛选（seed 2026）

已把逐维标准化参数保存为模型buffer；训练时只从train的有效音视频位置估计，验证/专项推理直接使用检查点统计量。所有模型都支持 `normalize_inputs`、`pack_aligned_grus` 两个开关，原检查点默认补零兼容。对齐音视频有效位置都位于原始文本长度以内，因而打包使用原始 attention 长度，内部缺口不压缩。FUSE用同seed、同核心优化超参数和四个固定sample_v2验证视图做了四组筛选；联合组的训练轮数/patience更长，但四组均在epoch2达到最佳。验证选择分数越低越好。

| 筛选组 | 选择分数 | clean F1 | clean MAE | local F1 / MAE | interval F1 / MAE |
|---|---:|---:|---:|---:|---:|
| 关闭两项（sample_v2控制） | 0.3073 | 0.5963 | 0.5800 | 0.5965 / 0.5806 | 0.5443 / 0.6400 |
| 仅音视频标准化 | **0.2967** | **0.6192** | **0.5739** | **0.6133 / 0.5763** | **0.5730 / 0.6335** |
| 仅打包GRU | 0.3087 | 0.6020 | 0.5868 | 0.5950 / 0.5876 | 0.5476 / 0.6509 |
| 标准化＋打包 | 0.2975 | 0.6153 | 0.5767 | 0.6116 / 0.5785 | 0.5707 / 0.6396 |

按当前单seed筛选，标准化相对同轮次、同验证协议控制组提升2.28个百分点clean F1、MAE下降0.0061；打包单项退化，两项组合不如标准化单项。打包减少尾部padding影响，却没有转化为当前任务的泛化收益，先不默认启用。标准化是有希望的候选，但尚未跨seed复核，不能宣称超过当前历史最佳。

历史FUSE两种子集成在相同sample_v2视图重算后clean F1/MAE仍为0.6328/0.5729，四视图分数约0.2886；它的checkpoint是在旧验证视图下选择的，与新控制组训练选择口径不同。seed2028配对验证已完成；历史最佳仍需按统一训练与选模规则重新比较。

#### P1 配对复核（seed 2027）和两种子集成

用相同sample_v2验证seed2026、同一seed下的控制组和标准化组完成第二次训练复核。验证视图选择结果：

| 训练seed | 组别 | 选择分数 | clean Macro-F1 | clean MAE | clean Pearson |
|---:|---|---:|---:|---:|---:|
| 2026 | 无改动控制 | 0.3073 | 0.5963 | 0.5800 | 0.6616 |
| 2026 | 仅标准化 | **0.2967** | **0.6192** | **0.5739** | **0.6715** |
| 2027 | 无改动控制 | 0.3112 | 0.5922 | 0.6076 | 0.6332 |
| 2027 | 仅标准化 | **0.3017** | **0.6154** | **0.5752** | **0.6528** |
| 2028 | 控制组 | 0.3159 | 0.5792 | 0.6063 | 0.6202 |
| 2028 | 仅标准化 | **0.3040** | **0.5994** | **0.5990** | **0.6383** |

三个seed中标准化相对配对控制的clean指标均改善，配对平均Macro-F1 +0.0221、MAE -0.0152、Accuracy +0.0224、Pearson +0.0159；三个seed的四视图选择分数也均下降。seed2028控制组与标准化组的clean Accuracy/F1/MAE/Pearson分别为0.5797/0.5792/0.6063/0.6202和0.5975/0.5994/0.5990/0.6383，四视图选择分数从0.3159降至0.3040。seed2026/2027两标准化检查点集成 clean F1/MAE 为0.6159/0.5660，四视图选择分数0.2969；配对控制集成分别为0.6066/0.5776、分数0.3012。标准化对新训练控制基线的收益已在三组配对中复现。

该结论仍有边界：seed2026/2027标准化集成的Macro-F1仍低于旧FUSE两种子集成的0.6328，MAE改善约0.0069，Accuracy下降约2.47个百分点。旧检查点用不同验证视图选择，因此不将其与新训练作为完全同条件的排名。标准化可保留为有三seed支持的候选输入处理；要替换历史最佳，需要统一训练与选模协议重新比较。

#### P2 signed回归头筛选（seed 2026）

在标准化输入上只把回归模式从soft改为signed，分类与回归输出分开。完整sample_v2评估的四视图选择分数为0.2940，clean Accuracy/F1/MAE/Pearson 为0.6360/0.6241/0.5828/0.6721。相对同seed标准化soft组，clean F1 +0.0049，但MAE恶化0.0089；总体分数低约0.0027主要受local/whole/interval影响。对强幅度回归并未胜过标准化soft，不能只凭F1选择。此为单seed初筛，尚未多seed验证。

首次训练在第6轮后因某个batch没有有效跨模态配对而未返回 `contrast` 日志项，触发KeyError。已把无此项批次按0计入汇总，损失本身保持原样。重跑完成早停；完整训练的联合组最佳epoch=2，10轮早停。训练集损失持续下降而验证恶化，过拟合仍然是主要限制。四组完整逐视图结果及检查点位于 `outputs/diagnostics/bottleneck_audit_20260924/`；配置分别是 `q2_fuse_samplev2_control.yaml`、`q2_fuse_norm_only.yaml`、`q2_fuse_pack_only.yaml`、`q2_fuse_norm_pack.yaml`。

### 3.2 分类瓶颈集中于中性，回归瓶颈集中于强情感

验证集负/中/正分别206/184/338条。中性184条，加弱非零情感147条，共45.47%。分类标签与回归值符号全部一致，未发现把中性并入正向的读取错误。

FUSE负/中/正三类F1分别0.6833 / **0.4987** / 0.7164。中性与弱情感边界是主要分类难点。

| FUSE错误分组 | 样本数 | 分类准确率 | MAE |
|---|---:|---:|---:|
| 中性 y=0 | 184 | 0.5217 | 0.3617 |
| 弱非零 0<abs(y)≤0.5 | 147 | 0.4762 | 0.3811 |
| 中等 0.5<abs(y)<2 | 323 | 0.7399 | 0.5995 |
| 强情感 abs(y)≥2 | 74 | **0.9189** | **1.3636** |

强情感样本的真实平均绝对强度为2.2545，预测仅0.9672；方向大多正确，幅度严重收缩。

当前soft输出为 `y_hat = 3*sigmoid(g) * (p_positive-p_negative)`。当分类概率较平缓时，幅度被限制；例如概率差0.35时，无论回归支路如何变化，强度绝对值都不超过1.05。该机制与观察到的收缩一致，但尚不能认定它是唯一原因，小样本、损失形式也可能影响幅度。

下一步：在相同输入和固定验证缺失下，对照已有 `regression_mode: signed` 的独立回归头；保留三分类头及真实零标签。随后单独比较L1和当前Smooth-L1，记录中性、弱情感、强情感的得失。不要先通过扩大幅度或提高极端样本权重硬推总指标，也不要给非零标签设置“中性容差带”。

### 3.3 CICA已经有监督与冻结，问题在监督效果和检查点选择

当前代码有单模态任务损失、误差监督的不确定性、MCP、CAP/CIF参数隔离。不能再把下降归因于“完全没加损失”或“仍在端到端训练”。

新CICA-BiGRU的完整训练集 Macro-F1/MAE 为 **0.8526/0.4003**，验证集为 **0.5952/0.6077**。相比之下，FUSE训练集约0.6930/0.5382，验证集0.6328/0.5729。CICA存在明显泛化差距；CIF只训练少数轮并不代表CAP没有过拟合。

新CICA的CAP分支在验证集上：

| 模态 | 单模态准确率 | 平均置信度 | 置信度与预测正确性的相关 | 不确定性与绝对误差的相关 |
|---|---:|---:|---:|---:|
| 文本 | 0.6250 | 0.9534 | -0.0531 | 0.2185 |
| 音频 | 0.4368 | 0.9316 | 0.1279 | 0.1608 |
| 视觉 | 0.4123 | 0.9548 | 0.0196 | 0.0724 |

“输出了置信度”没有等于“能区分可靠样本”。目前校准项对每条样本都以1为目标，倾向饱和；这也见于原论文式(6)，并非发现漏抄了一个标签项。适配本任务时需要验证该目标，而不是以忠于公式代替有效性证据。误差头有监督，但误差排序能力偏弱。

另一个明确问题：CAP以三分支平均分数选一个公共epoch后整体冻结。新BiGRU选中epoch6，文本分支F1/MAE为0.5877/0.6573；文本自身综合最优在epoch3，达到 **0.6105/0.5982**。声学、视觉各自最优又是11和7。共同早停导致并非最佳的文本编码器被永久冻结。现有日志没有这些各分支逐轮权重，需要重训才能取回。

下一步保持两阶段：每个分支独立保存最佳编码器、预测头及可靠性头，再组装冻结。先做 `r=1、MCP=0` 的任务基线，再分别加入可靠性与MCP。置信度可试用预测正确性/连续误差构造监督，校准仅在train内部按源视频分组留出或交叉预测，不用验证标签拟合可靠性头。可靠性是否保留由校准误差、错误检出能力、融合收益共同决定。

MCP当前直接让同一个融合向量与各冻结模态向量做对比，缺少独立投影空间，批内同情感样本也可能成为负例。它可能迫使融合表示保留弱模态无关因素；这是需要 `MCP=0` 消融确认的机制假设。若恢复MCP，先小权重、加投影并排查同类假负例；不应直接增大权重来“防坍缩”。本工程也不是原CICA的完整复现：原论文还有Transformer/MixDomainAdapter和跨模态注意力细节。

### 3.4 声画有无作用应通过消融测量

对同一FUSE检查点去掉音频和视觉，验证F1从0.6328降至0.6159，MAE从0.5729升至0.5809。声画确有增益，但规模约1.69个百分点F1和0.008 MAE。新CICA去掉声画反而从F1 0.5952升至0.5992，说明它尚未有效利用声画。

不能依据门控音频权重0.007就断言网络失败，也不能把权重推到1/3作为优化目标。应看同一模型遮蔽声画后，错误是否增加，以及哪些类别、缺失区间、强度范围受益。

固定参数的线性诊断探针也没有发现简单可分的声画捷径：音频均值/标准差探针验证F1约0.336，视觉约0.344；文本均值约0.542。它们不是声画性能上限，只说明简单汇聚不足以解决本题。

后续融合建议先试文本基础预测加轻量声画残差：让声画补充文本仍不确定或强度不足的部分，而不是让弱分支竞争替代整段文本表示；声画缺失时修正为零，文本缺失时保留明确的声画回退分支。使用低权重单模态监督，并以消融检验条件增益。该方案尚未实现或证明优于FUSE。

### 3.5 旧缺失验证随batch size和训练种子改变

旧代码每批抽一次局部缺失率和随机种子。同一验证集、同一seed，batch32与64的local/interval缺失掩码分别有94.37%/95.19%的样本不同。训练评估用 `训练seed+9001`，独立评估用2026，也让不同训练种子面对不同损坏输入。

本次新增 `sample_v2`：用验证seed、视图名、样本ID生成稳定种子；缺失位置不依赖batch划分和样本顺序。训练脚本和独立评估均支持，验证seed与训练seed分开。历史默认 `legacy_batch` 保留用于重现。

新旧local/interval指标不能直接比较；clean与整段AV缺失不受该抽样更改影响。`scripts.robustness` 的旧扫描目前仍是旧抽样口径，后续如使用该扫描做选模，需要同样迁移，不能与sample_v2主评估混称一个协议。

## 4. 已实现并实测的小改进

新增可选 `neutral_zero`：在单模型或集成完成后，如果预测三分类为中性，将最终情感强度设为零。它不修改标签、类别概率、负/正样本输出，也不重新训练。选择该规则用到了验证结果，不能作为独立泛化证明。

相同FUSE权重、相同sample_v2缺失输入：

| 视图 | 原MAE | 中性置零MAE | 原Pearson | 中性置零Pearson |
|---|---:|---:|---:|---:|
| clean | 0.5729 | **0.5628** | 0.6737 | 0.6704 |
| local | 0.5725 | **0.5603** | 0.6735 | 0.6708 |
| whole AV | 0.5809 | **0.5703** | 0.6662 | 0.6604 |
| interval | 0.6062 | **0.5961** | 0.6302 | 0.6284 |

所有视图的Accuracy和Macro-F1不变。该规则降低MAE但略降Pearson，不是四项指标同时提升，因此只作显式可选配置，不替换所有默认输出。Q3默认预测和Shapley解释也没有自动切换；如果后续启用，解释必须针对同一个决策规则重新计算。

## 5. 问题三先补全解释依据

现有20条映射的token匹配率都是1.0，但这只验证50位置内词元身份一致，不验证声学时间准确，更不表示完整视频覆盖。

| 样本 | 映射到模型的对齐词 | 已映射最晚时间 | 转写最后对齐词时间 |
|---|---:|---:|---:|
| 07 | 36/51 | 15.364秒 | 21.046秒 |
| 18 | 39/46 | 15.254秒 | 18.116秒 |

本次已给对齐和解释输出增加 `evidence_scope`、映射词数、对齐词覆盖率、映射起止时间等字段；推理时也能对旧映射计算这些字段。`aligned_transcript` 仅表示已经有时间戳的转写词覆盖，不代表全视频时段，也不包括强制对齐失败而未被返回的词。另修正证据文本按字符串去重的问题：重复子词合并，但不同词位的“very very”等重复词保留。没有改写历史解释文件。

下一步解释流程：

1. 根据完整验证集选择预测器；保留精确8子集Shapley，分别解释类别margin和连续强度，显示正/负贡献。当前positive share仅表示正向支持份额，不能代替全部贡献。
2. 将相邻BERT子词按同一词ID合并，再做连续词/时间窗遮蔽，防止多个重叠子词窗口反复“发现”同一证据。文本遮蔽后继续重算冻结BERT上下文。
3. 验证集上做删除/插入曲线、充分性/完整性检查，与同长度随机窗口比较；分类和强度各自报告。这衡量解释对模型预测的忠实度，不证明人类情绪因果。
4. 对附件4音频时间戳做抽样人工核查，并标记映射失败、截断、整段零视觉等情况；输出视频关键帧作为证据展示，不借此拟合情感模型。
5. 题目没有人工证据时间段标签，不能报告没有真值支持的时间IoU或定位准确率。

### 是否应该立即换unaligned？

不是必要前提。本题unaligned的音视频为500步，训练集实际音频长度中位数127、视觉82，文本仍是50步；切换它不能自动补回被截断文本，也不能把 `位置/500×时长` 当成真实时间。提供的特征没有逐帧时间戳。

当前有6.89%的训练文本、6.04%的验证文本在完整BERT分词后超过50位，截断需要处理，但不是全部样本的瓶颈。训练/验证来源视频集合不重叠，也需要关注跨视频泛化。

只有在明确原特征采样时间，或在训练数据中学习可验证的跨粒度映射后，再引入原始较长音视频序列并统一训练/验证/专项流程。问题一新增F0、节奏、姿态等特征不能直接替换问题二74/35维输入：附件2缺少原始训练音视频，特征语义和分布必须一致。当前相邻词位声学差分不等于真实时间单位上的语速或音高变化率。

附件4第13条全零视觉继续标记缺失，不伪造视觉贡献，不单独把其未对齐特征塞入对齐模型。

## 6. 下一轮实验次序与停止条件

先以当前FUSE和基线各自为锚点，固定sample_v2、验证seed2026、同一数据处理、同一选模分数，逐步单变量试验。先单种子筛掉明显退化项，再对候选作2026/2027/2028三种子重复，分别汇总单模型均值/标准差和集成，避免把集成收益误归于架构。

| 顺序 | 试验 | 主要检验目标 | 状态 |
|---|---|---|---|
| P0 | 固定缺失评估、权重溯源、中性输出规则 | 比较公平；MAE与Pearson取舍 | 已实现、已验证 |
| P1a | 训练集逐特征标准化 | 弱模态独立质量、声画消融收益 | 已实现；配对seed2026/2027均优于新训练控制，仍需与历史最佳同协议比较 |
| P1b | 音视频与融合GRU有效长度pack | padding不变性、泛化 | 已实现；单seed筛选为负，默认关闭 |
| P2 | 同骨干独立分类/回归头，随后单独比较L1 | 强情感幅度与中性误差 | signed seed2026已初筛，分类/回归指标有取舍，待多seed和L1控制实验 |
| P3 | CAP独立最佳分支；r=1、MCP=0起步 | 去除劣质表征冻结、校准与MCP负迁移 | 待实现和重训 |
| P4 | 轻量声画残差及单模态辅助监督 | 对文本错误的条件修正 | 待实现和重训 |
| Q3 | 单词级证据与删除/插入评估 | 证据忠实度与真实覆盖 | 覆盖字段已补；其他待实现 |

Q2的下一版选模建议预先固定clean/local/interval为主，whole单列压力结果；Q3预先固定clean预测为主。改变选模规则后应对候选统一重新选择检查点，不将新旧选择分数混排。不能根据附件3/4的具体缺失比例反复调整增强；以题面定义和train/valid实验决定参数，专项样本只做格式核查、缺失识别与最终推断。已有历史配置含参考附件3分布的设计，应在实验记录中如实注明，不能声称整个开发过程从未查看专项特征。

验收除总体指标外，必须检查中性F1、强情感MAE、文本缺失表现、声画消融增益。若改动只增大门控权重而不改善这些指标，或只改善训练集，应停止叠加。三种子结果与验证样本成对差异可用于不确定性分析，但反复用同一验证集选方案仍会带来选择偏差；不把微小差异包装成显著突破。

## 7. 运行与产物

在工程根目录、pytorch环境运行。本次完成7组检查点验证诊断、3组训练集诊断、4组固定线性探针，以及同一FUSE两种输出规则的四视图验证；没有启动新的神经网络完整训练。

```powershell
# 重新生成完整诊断，包含现有指定检查点的比较。
& 'C:\Anaconda3\envs\pytorch\python.exe' -m analysis.audit_bottlenecks

# 新协议下的中性置零评估。
& 'C:\Anaconda3\envs\pytorch\python.exe' -m scripts.evaluate --config config/q2_fuse_neutral.yaml

# 同协议原始输出对照；单独保存。
& 'C:\Anaconda3\envs\pytorch\python.exe' -m scripts.evaluate --config config/q2_fuse_neutral.yaml --no-neutral-zero --output outputs/diagnostics/bottleneck_audit_20260924/fuse_raw_sample_v2.json --per-sample outputs/diagnostics/bottleneck_audit_20260924/fuse_raw_sample_v2.csv

# 后续基线/FUSE训练用固定协议和新输出目录；当前配置已经写入这两个参数。
# 要复核标准化，按相同seed配对训练控制组和标准化组，改seed/output-dir。
# scripts.train_cica 同样支持固定验证协议参数。

& 'C:\Anaconda3\envs\pytorch\python.exe' -m unittest discover -s tests
```

诊断输出均在 `outputs/diagnostics/bottleneck_audit_20260924/`；原始数据、历史检查点和结果没有覆盖。7项单元检查覆盖缺失掩码对批量/顺序不变、验证seed独立、中性输出策略、文本遮蔽上下文隔离、解释覆盖，以及train-only标准化和尾部padding不变性。

## 8. 文献依据及适用边界

- [CICA原文（用户提供PDF）](C:/Users/Orange/Downloads/Jiang_CICA_Coupling_Confidence-Aware_Pretraining_with_Confidence-Informed_Attention_for_Robust_Multimodal_CVPR_2026_paper.pdf)：核查CAP、冻结CIF、置信度式(6)、误差不确定性和MCP。论文机制是实验起点，本题小样本、三分类联合回归和特征接口均需重新验证。
- [Self-MM，AAAI正式论文](https://ojs.aaai.org/index.php/AAAI/article/download/17289/17096)：多任务、单模态监督提供思路；基准划分及二分类评估不能直接当作本题三分类目标。
- [Balanced Multimodal Learning via On-the-fly Gradient Modulation，CVPR 2022](https://arxiv.org/abs/2203.15332)：支持关注模态优化不平衡，但不能由此推出各模态应占相等权重。
- [Attention is not Explanation，NAACL 2019](https://aclanthology.org/N19-1357/)：注意力数值不能自动作为充分解释；本题应以输入扰动、贡献分解及证据忠实度验证补充。
